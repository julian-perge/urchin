
[build-system]
requires = ["uv_build>=0.10,<1"]
build-backend = "uv_build"

[project]
name = "urchin"
version = "0.1.0"
description = ""
requires-python = ">=3.14,<4"
authors = [{ name = "Julian Perge", email = "julianperge@gmail.com" }]
readme = "README.md"

dependencies = [
    "msgspec>=0.21.1",
    # portrait/face compositing in python_conversion_scripts/swf_extraction
    "pillow>=11.0",
]

[tool.uv]
package = false
